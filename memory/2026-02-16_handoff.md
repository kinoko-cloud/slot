# 2026-02-16 作業引き継ぎメモ

## 📋 本日の作業概要

### 問題：営業中に昨日の古いデータが表示される

**根本原因：**
1. recommender.pyが、営業中でも`data/history/`から昨日のデータにフォールバック
2. v2スクリプトの「G数変化なしスキップ」最適化により、多くの台でhistory・total_start未取得
3. data/history/は蓄積ワークフロー（23:00）でしか更新されないため、営業中は当日データなし

---

## ✅ 実施した修正

### 1. recommender.py修正（営業中は昨日データを使わない）

**ファイル：** `analysis/recommender.py`

**修正箇所1：** 2752-2771行目
```python
# 営業中（10-22時）は昨日データにフォールバックしない
if daily_data and not is_business_hours:
    # 営業時間外のみdaily_dataから取得
```

**修正箇所2：** 2900行目
```python
# 営業中（10-22時）は昨日のデータにフォールバックしない
if daily_data and today_analysis.get('status') == '-' and not is_business_hours:
```

**修正箇所3：** 2914-2939行目（history・total_games補完）
```python
# 営業中は、常にdata/history/から当日のhistoryとtotal_gamesを補完
if is_business_hours:
    today_str = datetime.now().strftime('%Y-%m-%d')
    for day in _hist_unit_data.get('days', []):
        if day.get('date') == today_str:
            # historyがない、または空の場合に補完
            if not today_analysis.get('today_history') or len(today_analysis.get('today_history', [])) == 0:
                today_analysis['today_history'] = day.get('history', [])
            # total_gamesが0の場合に補完
            if today_analysis.get('total_games', 0) == 0:
                today_analysis['total_games'] = day.get('total_start', 0) or day.get('games', 0)
            break
```

### 2. v2スクリプト修正（スキップ無効化）

**ファイル：** `scripts/scrapers_v2/fetch_all.py`

**修正箇所：** 129-132行目
```python
# G数が変化した台を特定
changed_units = get_changed_units(store_key, games_map)

# 🚨 一時的に全台取得（スキップ無効化）
changed_units = set(expected_units)
```

**効果：**
- 全183台のhistory・total_startを強制取得
- 約13分で完了（21:12-21:25）

### 3. 並列ワークフロー復活

**ファイル：** `.github/workflows/fetch-availability-parallel.yml`
- 無効化されていた並列ワークフローを復活
- v2ワークフローを無効化

**ファイル：** `.github/workflows/fetch-availability-v2.yml`
- v2ワークフローを無効化（並列に移行）

---

## 🎯 現在の状況

### データ取得状況
- ✅ 最終取得：21:25（全183台）
- ✅ fetched_at: 2026-02-16T21:25:57+09:00
- ✅ 全台のhistory・total_start取得済み

### デプロイ状況
- ✅ 最終プッシュ：21:30頃（コミット: 7f5a3cc1c7）
- 🔄 Cloudflare Pagesデプロイ中（約2-3分）

### 期待される結果
- ✅ 営業中は昨日のデータを表示しない
- ✅ 全台でグラフ（history）が表示される
- ✅ 全台でスタート数（total_games）が表示される
- ✅ リアルタイムデータのみ使用

---

## ⚠️ 重要な注意点

### 1. v2スクリプトのスキップ無効化は一時的

**現状：**
```python
# 🚨 一時的に全台取得（スキップ無効化）
changed_units = set(expected_units)
```

**問題：**
- 全台取得は時間がかかる（約13分）
- 本来は「G数変化なしスキップ」で高速化していた

**今後の対応が必要：**
1. **オプションA：** スキップを恒久的に無効化（毎回13分かかる）
2. **オプションB：** スキップ時もhistoryを保持する仕組みを実装
3. **オプションC：** 並列ワークフローに完全移行（各店舗8分、並列実行）

### 2. data/history/の制約

**問題：**
- data/history/は蓄積ワークフロー（23:00）でしか更新されない
- 営業中は当日データがない
- → historyとtotal_gamesの補完が機能しない場合がある

**現在の対策：**
- v2スクリプトで全台取得してavailability.jsonにhistory保存
- recommender.pyでavailability.jsonのhistoryを優先使用

### 3. 並列ワークフローの状況

**復活させたが、まだ実行されていない**
- 次回実行：22:00（JST）
- その後は毎時0分に自動実行

---

## 📝 残タスク

### 優先度：高

1. **デプロイ確認**
   - サイトで全台のグラフ・スタート数が表示されるか確認
   - 昨日のデータが表示されていないか確認

2. **v2スクリプトのスキップ処理の恒久対策**
   - 現在は一時的に無効化（全台取得）
   - 長期的な解決策を決定・実装

3. **並列ワークフローの動作確認**
   - 22:00の自動実行を確認
   - エラーがないか確認

### 優先度：中

4. **営業時間外のデータ表示**
   - 23:00以降は、静的HTMLモードに切り替わる
   - 正しく動作するか確認

5. **history補完ロジックの最適化**
   - 現在はdata/history/から当日分を補完
   - より効率的な方法を検討

### 優先度：低

6. **CSS未使用クラスの整理**
   - 279件の未使用クラスが蓄積
   - 必要に応じて整理

---

## 🔍 デバッグ方法

### サイトで問題が見つかった場合

1. **availability.jsonを確認**
   ```bash
   python3 -c "
   import json
   with open('data/availability.json') as f:
       data = json.load(f)
   print('fetched_at:', data.get('fetched_at'))
   # 特定店舗の確認
   units = data['stores']['shibuya_espass_hokuto2']['units']
   for u in sorted(units, key=lambda x: x.get('art', 0), reverse=True)[:3]:
       print(f\"{u['unit_id']}: {u['art']}ART, {u['total_start']}G, history={len(u.get('history', []))}件\")
   "
   ```

2. **data/history/を確認**
   ```bash
   python3 -c "
   import json
   from pathlib import Path
   hist = json.loads(Path('data/history/shibuya_espass_hokuto2/2052.json').read_text())
   for day in hist['days'][-3:]:
       print(f\"{day['date']}: {day.get('art', 0)}ART, {day.get('total_start', 0)}G, history={len(day.get('history', []))}件\")
   "
   ```

3. **HTML生成ログを確認**
   ```bash
   python3 scripts/generate_static.py 2>&1 | grep -A 5 "POST-BUILD"
   ```

### 全台データを再取得したい場合

```bash
# v2スクリプトで全台取得（約13分）
timeout 900 python3 scripts/scrapers_v2/fetch_all.py

# HTML再生成
timeout 300 python3 scripts/generate_static.py

# コミット・プッシュ
git add data/availability.json docs/
git commit -m "update: 全台リアルタイムデータ更新"
git push origin main
```

---

## 📚 関連ファイル

### 主要ファイル
- `analysis/recommender.py` - 推奨ロジック（営業中の昨日データフォールバック制御）
- `scripts/scrapers_v2/fetch_all.py` - v2データ取得スクリプト（スキップ制御）
- `scripts/generate_static.py` - HTML生成
- `.github/workflows/fetch-availability-parallel.yml` - 並列ワークフロー
- `.github/workflows/fetch-availability-v2.yml` - v2ワークフロー（無効化済み）

### データファイル
- `data/availability.json` - リアルタイムデータ
- `data/history/<store_key>/<unit_id>.json` - 蓄積データ（23:00更新）

### ドキュメント
- `CLAUDE.md` - プロジェクト仕様
- `ARCHITECTURE.md` - コードベース構造
- `memory/MEMORY.md` - 長期メモリ
- `memory/2026-02-16_handoff.md` - 本ファイル

---

## 💡 重要な仕様

### 営業中のデータ表示
- **営業時間：** 10:00-22:00（JST）
- **データソース：** availability.json（リアルタイム）
- **昨日データ：** 使わない（メインデータ）
- **history補完：** data/history/から当日分のみ
- **total_games補完：** data/history/から当日分のみ

### 営業時間外のデータ表示
- **時間帯：** 23:00-9:59（JST）
- **データソース：** data/history/（蓄積データ）
- **表示モード：** 静的HTML（前日結果）

---

## 🚀 次回作業開始時のチェックリスト

1. [ ] サイトでデータが正しく表示されているか確認
2. [ ] 並列ワークフローが22:00に実行されたか確認
3. [ ] availability.jsonのfetched_atが最新か確認
4. [ ] v2スクリプトのスキップ処理の恒久対策を決定

---

**作業者:** Claude Sonnet 4.5
**作業日時:** 2026-02-16 15:00-21:30
**最終コミット:** 7f5a3cc1c7
**最終プッシュ:** 2026-02-16 21:30
