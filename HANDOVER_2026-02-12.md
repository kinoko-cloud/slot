# 引き継ぎ文書 2026-02-12

## 本日実装したこと

### 1. 古いデータ警告システム
**ファイル:** `scripts/generate_static.py`, `web/templates/index.html`

- 営業時間中（10:00-23:00）に以下を検出：
  - fetched_atの日付が今日でない
  - データが60分以上古い
- フロントエンドに赤い警告バナーを表示
- `is_data_stale`, `data_stale_reason` 変数を追加

### 2. 朝のプリフェッチ追加
**ファイル:** `.github/workflows/auto-recovery.yml`

- 9:55（JST）にプリフェッチ実行
- 10:05（JST）に強制チェック
- 日付チェック追加（fetched_atが今日でなければ即エラー→re-fetch）

### 3. GitHub Actions冗長化
**ファイル:** `.github/workflows/*.yml`

毎時4回の冗長実行体制：
| 時刻 | ワークフロー |
|------|-------------|
| :00 | fetch-parallel（データ取得） |
| :05 | deploy-static（古ければ自動fetch） |
| :15 | auto-recovery（自動復旧） |
| :30 | fetch-parallel（データ取得） |
| :35 | deploy-static（古ければ自動fetch） |
| :45 | auto-recovery（自動復旧） |

### 4. health_check.py修正
- island_akihabara関連のエラーを除外
- 閾値を緩和

---

## 未完了・次回着手

### 1. 並列化 + リトライ方式への改善
**対象:** `scripts/batch_update_history.py`

RSさんの指摘：
> 長いタイムアウトより、並列数を可変で減らしたり、リトライ回数の方がいいのでは？

改善案：
- 並列数を可変（5→3→1と段階的に）
- 短タイムアウト（30秒）+ リトライ3回
- 部分保存（10台ごとに保存、失敗しても途中まで残る）

現状：1台ずつ順番処理で30分以上かかる

### 2. 2/12履歴データ取得
74/76台まで完了、残り：
- akiba_espass_hokuto2: 2067, 2068番
- papimo（アイランド）: 4台

実行コマンド：
```bash
cd /home/riichi/works/slot
python3 scripts/batch_update_history.py --target-date 2026-02-12
```

---

## 今日の問題と原因

### 1. データ取得タイムアウト
- 76台の履歴取得に30分以上かかる
- 5分タイムアウトで何度も中断
- 原因：1台ずつ順番処理、サイトの応答遅延

### 2. 朝の前日データ残り問題
RSさんの指摘：
> 毎日毎日毎日、営業時間はじまると、前日だったり、いつのか分からないデータが入っていたりして、それを直す指示を見るたびにしてます

対策（実装済み）：
- 9:55プリフェッチ
- 10:05強制チェック
- フロントエンド警告

---

## 確認コマンド

```bash
# 履歴データの状況確認
cd /home/riichi/works/slot
python3 << 'EOF'
import json
from pathlib import Path
from collections import defaultdict

history_dir = Path('data/history')
store_stats = defaultdict(lambda: {'dates': set(), 'units': 0})

for store_dir in history_dir.iterdir():
    if not store_dir.is_dir():
        continue
    for unit_file in store_dir.glob('*.json'):
        try:
            with open(unit_file) as f:
                data = json.load(f)
            store_stats[store_dir.name]['units'] += 1
            for day in data.get('days', []):
                if day.get('date'):
                    store_stats[store_dir.name]['dates'].add(day['date'])
        except:
            pass

for sk in sorted(store_stats.keys()):
    stats = store_stats[sk]
    dates = stats['dates']
    has_0212 = '2026-02-12' in dates
    mark = '✅' if has_0212 else '❌'
    print(f"{mark} {sk}: {stats['units']}台, {min(dates) if dates else 'N/A'}～{max(dates) if dates else 'N/A'}")
EOF

# availability.jsonの鮮度確認
cat data/availability.json | python3 -c "import sys,json; d=json.load(sys.stdin); print(f\"fetched_at: {d.get('fetched_at')}\")"

# GitHub Actions実行状況
gh run list --limit 5
```

---

## コミット履歴（本日）

```
d252299557 feat: 古いデータ警告 + 朝のプリフェッチ追加
```

---

## 次回やること優先順位

1. **残りの2/12履歴データ取得**（6台）
2. **batch_update_history.py の並列化+リトライ改善**
3. **動作確認**（明日10:00の営業開始時）
